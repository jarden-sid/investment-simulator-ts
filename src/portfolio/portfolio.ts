export const monteCarloSimulation = (
    weightings: number[],
    returns: number[],
    covariance: number[][],
    period: number,
    initialInvestment: number = 1,
    fee: number = 0.00,
    simulations: number = 1_000) => {
}